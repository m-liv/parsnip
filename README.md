# parsnip

In this project, we investigate whether inference-time prompting strategies can mitigate 
dialect-related performance gaps in LLM reasoning tasks. We use the EnDive benchmark.


## To Run

python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt